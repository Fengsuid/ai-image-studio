# License

SPDX-License-Identifier: AGPL-3.0-or-later

Canvas Core is the backend slice for the canvas workspace (service, routes, store, assistant, import-export). It is distributed under AGPL-3.0-or-later, the same terms as `apps/canvas-v2/`, so backend and frontend share license boundaries with the upstream `basketikun/infinite-canvas` lineage that the canvas migration plan permits.

Every JavaScript source file in this package carries an `SPDX-License-Identifier: AGPL-3.0-or-later` header. The `smoke:canvas-license-headers` smoke test enforces that invariant.

The full AGPL-3.0 license text is available from the Free Software Foundation and should be included verbatim if upstream source files are copied into this tree.
